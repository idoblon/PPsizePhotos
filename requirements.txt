# Direct dependencies only — pip resolves transitive deps automatically.
# NOTE: local AI fallback (rembg + onnxruntime + opencv) is intentionally
# excluded for Vercel — it pushes the bundle over the 500MB function limit
# and needs a model download at runtime. Background removal uses the
# Remove.bg API in production; locally you can `pip install rembg onnxruntime`
# to re-enable the fallback (code handles it as optional).
Flask==3.1.1
Flask-Limiter==3.10.1
Pillow==11.1.0
cloudinary==1.44.1
requests==2.32.4
python-dotenv==1.0.1
